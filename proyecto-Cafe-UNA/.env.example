# Desarrollo local
BACKEND_URL=http://localhost:5220/api

# Alternativa: backend en MonsterASP
# BACKEND_URL=http://cafeunaof.runasp.net/api

# Netlify / GitHub Secrets — misma variable para proxy y build:
# BACKEND_URL=http://cafeunaof.runasp.net/api
# (En Netlify el frontend en producción usa /api vía proxy automáticamente)
